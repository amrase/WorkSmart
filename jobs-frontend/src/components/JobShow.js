import React, { Component } from 'react'
import JobContainer from './HomeContainer'
import CompanyContainer from './CompanyContainer'
import UserContainer from './HomeContainer'

export default class JobShow extends Component {

    state = {
        job: null
    }

    render() {
        return (
            <div>
              1 Jobs Application
            </div>
        )
    }
}
